import { basename } from "path";

export const ROUTE_NAMES = {
    HOME : `/`,
    ABOUT: `${basename}/about`,
    EXPERIENCE:'/experience',
    PROJECTS:'/projects',
    RESUME:'/resume',
    CONTACTS:'/contacts'
}